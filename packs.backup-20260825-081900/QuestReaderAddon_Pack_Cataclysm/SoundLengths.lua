-- Example shard. tools/build_sound_packs.py generates one of these per pack,
-- named QuestReaderSoundLengths_Pack_<PackSuffix> so that multiple packs'
-- indexes never collide as globals when several are loaded at once.
QuestReaderSoundLengths_Pack_Cataclysm = {
}
