# Cached listfile (~150 MB) and derived index — rebuild with: voice_sources.py index
.cache/
wowhead/

# Python
__pycache__/
*.pyc
.venv*/

# Pipeline artifacts — every one is regenerated by the step that names it,
# and the audio directories run to gigabytes.
missing.txt
slice.txt
npcs.txt
npcs_partial.txt
cached_ids.txt
ids.txt
passages.json
passages_full.json
passages_partial.json
passages.csv
voicebank.json
voicebank_full.json
npcs_full.txt
reference-audio/
generated/
